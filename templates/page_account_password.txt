{assign var="page_title" value="Change your password"}
{include file="inc_head.txt"}

<div class="container">
	{if $error_oldpass_mismatch}	<p class="alert alert-error">Incorrect old password.</p>{/if}
	{if $error_newpass_empty}	<p class="alert alert-error">Please choose a new password.</p>{/if}
	{if $error_newpass_mismatch}	<p class="alert alert-error">Your new passwords need to match.</p>{/if}
	{if $error_fail}		<p class="alert alert-error">Unknown failure :(</p>{/if}

	<form action="/account/password/" method="post" accept-encoding="UTF-8" class="form-horizontal">
		<input type="hidden" name="change" value="1" />
		{$crumb_key|crumb_input}

		<fieldset>
	        <div class="control-group">
				<label class="control-label" for="old_password">your current password</label>
	        	<div class="controls"><input type="password" name="old_password" size="40" value="" />
				</div>
			</div>

	        <div class="control-group">
				<label class="control-label" for="new_password1">your new password</label>
	        	<div class="controls"><input type="password" name="new_password1" size="40" value="" />
				</div>
			</div>

	        <div class="control-group">
				<label class="control-label" for="new_password2">your new password (again)</label>
	        	<div class="controls"><input type="password" name="new_password2" size="40" value="" />
				</div>
			</div>
			
	        <div class="form-actions">
	        	<input type="SUBMIT" class="btn btn-primary" value="CHANGE MY PASSWORD" />
			</div>
		</fieldset>
	</form>
</div>
	
{include file="inc_foot.txt"}
